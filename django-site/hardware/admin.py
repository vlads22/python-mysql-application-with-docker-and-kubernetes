from django.contrib import admin

# Register your models here.

#we need to tell the admin that Question objects have an admin interface. so they re available in django admin site
# from .models import Question
# admin.site.register(Question)
